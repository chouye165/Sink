# 2026-07-14

## Task: Public short-link generator + dashboard source filter (Sink)

Changed the Sink link-shortener so the landing page is a public, no-login short link
generator, and the dashboard can filter links by who created them (console/admin vs guest).

### Changes

- `shared/schemas/link.ts`: added `source: z.enum(['admin','guest']).default('admin')` to `LinkSchema`.
- `server/utils/link-processing.ts`: `mergeEditableLink` now preserves `source` from `existingLink` (admin edits don't reattribute guest links).
- `server/utils/link-store.ts`: `listLinks` accepts optional `source` filter; loops KV pages until `limit` matches collected; normalizes legacy links (missing source -> 'admin').
- `server/api/link/list.get.ts`: accepts `source` query param, forwards to `listLinks`.
- `server/middleware/2.auth.ts`: bypass auth for `POST /api/link/guest` (sets `authMethod='guest'`).
- `server/api/link/guest.post.ts` (NEW): public, unauthenticated creation; restricted schema (url + optional slug/comment only; strips extra keys); marks `source:'guest'`; runs safe-browsing check via `prepareIncomingLink`.
- `app/pages/index.vue`: replaced marketing page with public generator form (URL + optional custom slug, randomize, copy result). i18n keys under `home.*`.
- `layers/dashboard/app/composables/links.ts`: added `sourceFilter` ('all'|'admin'|'guest') to `useDashboardLinksStore`.
- `layers/dashboard/app/components/dashboard/links/Index.vue`: passes `source` to list API; resets+reloads on filter change.
- `layers/dashboard/app/components/dashboard/links/SourceFilter.vue` (NEW): dropdown filter (All / Console / Guest).
- `layers/dashboard/app/pages/dashboard/links.vue`: added `<DashboardLinksSourceFilter />` to header.
- `layers/dashboard/app/components/dashboard/links/Link.vue`: shows a source Badge (Console/Guest) on each card.
- `shared/types/link.ts`: excluded `source` from `LinkFormFields` (admin editor must not send it).
- i18n: added `home.*`, `links.filter.*`, `links.source.*` to all 11 locale files (zh-CN/zh-TW translated; others English fallback).

### Notes / follow-ups

- Public guest endpoint has NO rate limiting — recommend adding per-IP rate limiting before production exposure.
- Could not run `pnpm types:check` / `lint:fix`: node_modules/.nuxt were absent; ran `pnpm install` in background.

## Fix round 2: home page chrome + guest-filter duplicate

User reported: home shows top/bottom chrome and a console link; filtering dashboard by guest duplicated entries.

### Changes

- `app/layouts/blank.vue` (NEW): chrome-free layout (`<div><slot/></div>`).
- `app/pages/index.vue`: `definePageMeta({ layout: 'blank' })`; removed its own `<header>` (had the 控制台/Dashboard link) and `<footer>`; page now shows ONLY the generator card, centered. Removed now-unused `Link2` import.
- `layers/dashboard/app/components/dashboard/links/Index.vue`: added `isFetching` guard in `getLinks()` so the source-filter watcher and `useInfiniteScroll` can't concurrently fetch page 1 with the same stale cursor (the cause of duplicate guest links).

### Validation

- `pnpm lint:fix` passes (exit 0). `pnpm types:check`: 0 errors in changed files (8 pre-existing errors remain only in untouched `Search.vue`).

## Fix round 3: 404 page chrome

- `app/error.vue`: changed `<NuxtLayout name="default">` -> `name="blank"` so the 404/error page also omits the top bar and footer (reuses the chrome-free blank layout). Lint passes.

## Task: Dashboard "首页设置" (Homepage Settings) + captcha toggle

User wants a console settings page to enable/disable the public homepage generator and the captcha, and for a disabled homepage to show the existing 404 page.

### Changes
- `server/utils/site-config.ts` (NEW): `SiteConfigSchema` = { homepageEnabled: bool default true, captchaEnabled: bool default true }; `getSiteConfig`/`setSiteConfig` backed by KV key `config:site`.
- `server/api/config.get.ts` (NEW): public GET (returns the two toggles; non-sensitive).
- `server/api/config.put.ts` (NEW): admin PUT; merges partial with existing config.
- `server/middleware/2.auth.ts`: bypass auth for `GET /api/config` (authMethod='public'); hoisted the `/^Bearer\s+/` regex to module-scope `BEARER_REGEX` (fixes a `prefer-static-regex` lint error in that file).
- `app/pages/index.vue`: fetches `/api/config` on mount; if `homepageEnabled===false` -> `showError({statusCode:404})` (renders existing 404 page); if `captchaEnabled===false` skips the captcha gate (sets captchaVerified=true); added a loading spinner while config loads.
- `layers/dashboard/app/composables/dashboard.ts`: added `home` route (icon `Home`, paths `/dashboard/settings`, titleKey `nav.home_settings`).
- `layers/dashboard/app/components/dashboard/sidebar/AppSidebar.vue`: added `首页设置` to the settings nav group.
- `layers/dashboard/app/pages/dashboard/settings.vue` (NEW): settings page (relative import of the component).
- `layers/dashboard/app/components/dashboard/settings/HomeSettings.vue` (NEW): two `Switch` toggles (homepage enabled, captcha enabled) reading via GET and updating via PUT; "saved" toast.
- i18n: added `nav.home_settings` to all locales; new `settings.home.*` block (title/description/homepage_enabled[+_desc]/captcha_enabled[+_desc]/saved) in all 11 locales.

### Validation
- `pnpm lint:fix`: 0 errors in all changed files. Remaining 15 `e18e/prefer-static-regex` lint errors + 8 `Search.vue` type errors are all in PRE-EXISTING, UNTOUCHED upstream files (environmental from the forced reinstall; not introduced by this work).
- `pnpm types:check`: 0 errors in changed files.

### Environment notes (important)
- **package.json had a duplicate `devDependencies` key** (introduced when react/react-dom/playcaptcha were added in the captcha task). This made pnpm drop eslint and most devDeps from the install, breaking `pnpm lint`/`types:check`. Fixed by merging `@types/react`/`@types/react-dom` into the single `devDependencies`. If tooling later "disappears", check for duplicate top-level JSON keys first.
- `pnpm install` postinstall (`build:map`) fails in this sandbox with `ERR_MODULE_NOT_FOUND: @unovis/ts` — pre-existing/unrelated. Use `pnpm install --ignore-scripts` then `pnpm exec nuxt prepare` (sandbox off) to get a working `.nuxt` for type-check.
- `app/components/PlayCaptcha.client.vue` (captcha task): originally had TS errors on the React root; fixed by typing `root` as `import('react-dom/client').Root` and casting `ClawCaptcha as React.ComponentType<Record<string, unknown>>`.

## Task: Homepage captcha language switcher (中英切换)

User wants a language switch button on the homepage captcha UI so the captcha text switches between Chinese and English.

### Changes
- `app/components/PlayCaptcha.client.vue`: derive PlayCaptcha `language` prop from current i18n `locale` via `captchaLanguage()` — `zh-CN`->`zh`, `zh-TW`->`zh-TW`, `en-US`->`en`, others->primary subtag (e.g. `fr-FR`->`fr`). Was hardcoded `'zh'`.
- `app/pages/index.vue`: added `<SwitchLanguage />` (existing component) fixed top-right so it shows on the blank-layout homepage; added `:key="locale"` to `<PlayCaptcha />` so it re-mounts with the new language when the locale changes. Destructured `locale` from `useI18n()`.

### Notes
- Homepage already used `$t('home.*')` for all text, so the title/prompt/verified strings already switch; only the captcha widget text needed the locale mapping + re-mount.
- `SwitchLanguage` relies on `setLocale` (reactive, no navigation under `no_prefix` strategy) so `:key` re-mount + reactive `$t` is enough.
- Not run: `pnpm lint:fix`/`types:check` (node_modules/.nuxt absent). Follow environment notes above to validate.

